version: '3.8'

services:
  app:
    # Use 'build' to construct the image from the Dockerfile in the current directory.
    # This ensures the image architecture matches the server's architecture.
    build: .
    restart: unless-stopped
    networks:
      - gatochefnet
    # Traefik labels to expose the service
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.gato-chef-app.rule=Host(`inscripciones.gatochef.online`)"
      - "traefik.http.routers.gato-chef-app.entrypoints=websecure"
      - "traefik.http.routers.gato-chef-app.tls.certresolver=letsencryptresolver"
      # CRITICAL: This tells Traefik the service name and port inside the container.
      - "traefik.http.services.gato-chef-app.loadbalancer.server.port=3000"
    environment:
      - N8N_WEBHOOK_URL=${N8N_WEBHOOK_URL}
      - NEXT_PUBLIC_FIREBASE_API_KEY=${NEXT_PUBLIC_FIREBASE_API_KEY}
      - NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
      - NEXT_PUBLIC_FIREBASE_PROJECT_ID=${NEXT_PUBLIC_FIREBASE_PROJECT_ID}
      - NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}
      - NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}
      - NEXT_PUBLIC_FIREBASE_APP_ID=${NEXT_PUBLIC_FIREBASE_APP_ID}

networks:
  gatochefnet:
    external: true
